set number "show line numbers
set wrap "wrap lines
set encoding=utf-8 "set encoding to UTF-8
set mouse=a "enable mouse support
set wildmenu "visual autocomplete for command menu
set lazyredraw "redraw screen only when needed
set showmatch "highlight matching parentheses/brackets
set mat=2 "how often to blink each second if match found
set ruler "show line and column number of the courser

syntax enable "enable syntax highlighting
filetype plugin indent on "automatically detect filetypes

set noexpandtab "use tabs instead of spaces
set tabstop=1 "width that a TAB character displays as
set shiftwidth=0 "match tabstop for indentation

set autoindent "copy indent from current line when starting a new line
set smartindent "more indentation magic

set incsearch "search as charachters are entered
set hlsearch "highlight matches
set smartcase "when searching be smart about cases

set autoread "autoreload file if it changed outside vim

set t_Co=256 "enable 256 colors

set clipboard=unnamedplus "enable global clipboard
